{
    options: [
        {
            includeToken: "lumi:bloom_config",
            categoryKey: "config.lumi_lights.category.bloom",
            options: {
                bloom_intensity: {
                    default: 5,
                    min: 1,
                    max: 40,
                    nameKey: "config.lumi_lights.value.bloom_intensity",
                    descriptionKey: "config.lumi_lights.help.bloom_intensity"
                },
                sky_bloom_intensity: {
                    default: 5,
                    min: 0,
                    max: 10,
                    nameKey: "config.lumi_lights.value.sky_bloom_intensity",
                    descriptionKey: "config.lumi_lights.help.sky_bloom_intensity"
                },
                bloom_downsample_dist: {
                    default: 25,
                    min: 3,
                    max: 50,
                    nameKey: "config.lumi_lights.value.bloom_downsample_scale",
                    descriptionKey: "config.lumi_lights.help.bloom_downsample_scale"
                },
                bloom_upsample_dist: {
                    default: 5,
                    min: 1,
                    max: 50,
                    nameKey: "config.lumi_lights.value.bloom_upsample_scale",
                    descriptionKey: "config.lumi_lights.help.bloom_upsample_scale"
                },
                bloom_cutoff: {
                    default: 10,
                    min: 3,
                    max: 50,
                    nameKey: "config.lumi_lights.value.bloom_cutoff",
                    descriptionKey: "config.lumi_lights.help.bloom_cutoff"
                }
            }
        }
    ],
}
